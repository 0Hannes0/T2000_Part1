---
phase: 40-dokumentation
plan: 02
type: execute
wave: 2
depends_on:
  - 40-01
files_modified:
  - T2000_Part1/pages/appendix.typ
  - T2000_Part1/chapters/fundamentals-2.typ
autonomous: true
requirements:
  - DOKU-01
  - DOKU-02

must_haves:
  truths:
    - "pages/appendix.typ enthält einen Abschnitt A2 Systemparameter mit Tabelle aller 6 Parameter"
    - "Jeder der 6 Parameter hat Wert, Einheit und Kapitelreferenz in der Tabelle"
    - "IDLE, CANDIDATE und ACTIVE sind in fundamentals-2.typ beim ersten Auftreten mit Kurzerklärung definiert"
  artifacts:
    - path: "T2000_Part1/pages/appendix.typ"
      provides: "Anhang mit A2-Parametertabelle (6 Einträge)"
      contains: "Systemparameter"
    - path: "T2000_Part1/chapters/fundamentals-2.typ"
      provides: "Erstdefinition IDLE/CANDIDATE/ACTIVE im Gesamtarchitektur-Abschnitt"
  key_links:
    - from: "T2000_Part1/pages/appendix.typ"
      to: "T2000_Part1/template.typ"
      via: "#appendix(labels: labels) — Zeile 175 bindet appendix.typ ein"
    - from: "T2000_Part1/chapters/fundamentals-2.typ"
      to: "T2000_Part1/chapters/methodology.typ"
      via: "Definitionen in Kap. 3.1 gelten für alle IDLE/CANDIDATE/ACTIVE-Vorkommen in Kap. 4"
---

<objective>
Umsetzung: Anhang A2 „Systemparameter" in pages/appendix.typ anlegen mit vollständiger 6-Parameter-Tabelle; IDLE/CANDIDATE/ACTIVE-Erstdefinition in fundamentals-2.typ sicherstellen.

Purpose: DOKU-01 (Parametertabelle) und DOKU-02 (Terminologie-Einführung) erfüllen.
Output: appendix.typ erweitert, fundamentals-2.typ geprüft und ggf. ergänzt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-dokumentation/40-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anhang A2 Systemparameter in appendix.typ anlegen</name>
  <files>T2000_Part1/pages/appendix.typ</files>

  <read_first>
    T2000_Part1/pages/appendix.typ — gesamter aktueller Inhalt; versteht den Anhang-Aufbau (heading A1 KI-Werkzeuge, figure-table-Struktur)
    .planning/phases/40-dokumentation/40-AUDIT.md — die 6 Parameterwerte mit Einheit und Kapitelreferenz (Quelle der Tabellendaten)
    T2000_Part1/i18n/DE.typ — Zeilen 69–71 — labels.appendix-ai-heading und Spaltenstruktur als Stilvorlage
  </read_first>

  <action>
  Lies zuerst den gesamten Inhalt von pages/appendix.typ, dann das 40-AUDIT.md.

  Füge am Ende der appendix-Funktion (vor der schließenden `]`-Klammer) einen zweiten Anhangabschnitt ein:

  Heading-Struktur: `#heading(level: 2, numbering: none)[A2: Systemparameter]`

  Dann ein #figure mit kind: table, supplement: labels.supplement-table, und einem caption der lautet:
  "Konfigurierbare Parameter des Presence Service mit Standardwert, Einheit und Primärkapitel"

  Tabellenstruktur: columns: (auto, auto, auto, 1fr), d.h. 4 Spalten:
  Header: Parameter | Wert | Einheit | Kap.-Verweis

  Tabellenzeilen (exakt diese 6, in dieser Reihenfolge; Werte aus 40-AUDIT.md übernehmen):
  1. `CANDIDATE_SECS` | 4,0 | s | Kap. 4.3
  2. `LEAVE_SECS` | 10,0 | s | Kap. 4.3
  3. `SIMILARITY_THRESHOLD` | 0,52 | — | Kap. 5.1
  4. `FRAME_INTERVAL` | 1,0 | s | Kap. 4.1
  5. `GROUP_ARRIVAL_WINDOW_SECS` | 2,0 | s | Kap. 4.4
  6. EMA-α | 0,2 | — | Kap. 5.3

  Stil: stroke: 0.5pt, inset: (x: 6pt, y: 5pt), align: (left + top, left + top, left + top, left + top).
  Spaltenkopfzeilen fett (strong[...]).
  Parameternamen in Backtick-Syntax (`...`) außer EMA-α (kein Codeblock).

  Label: `<tab:systemparameter>`

  Wichtig: Die bestehende AI-Werkzeuge-Tabelle (A1) darf nicht verändert werden. Nur am Ende der Funktion anhängen.
  </action>

  <verify>
    <automated>grep -c "Systemparameter\|CANDIDATE_SECS\|GROUP_ARRIVAL_WINDOW_SECS\|EMA" "T2000_Part1/pages/appendix.typ"</automated>
  </verify>

  <acceptance_criteria>
  - pages/appendix.typ enthält den String "A2: Systemparameter"
  - pages/appendix.typ enthält alle 6 Parameternamen: CANDIDATE_SECS, LEAVE_SECS, SIMILARITY_THRESHOLD, FRAME_INTERVAL, GROUP_ARRIVAL_WINDOW_SECS, EMA
  - Die bestehende A1-Tabelle (ai-tools) ist unverändert vorhanden
  - Die Tabelle hat ein Label `<tab:systemparameter>`
  - `grep -c "CANDIDATE_SECS\|LEAVE_SECS\|SIMILARITY_THRESHOLD\|FRAME_INTERVAL\|GROUP_ARRIVAL_WINDOW_SECS\|EMA" "T2000_Part1/pages/appendix.typ"` liefert ≥ 6
  </acceptance_criteria>

  <done>appendix.typ enthält Abschnitt A2 mit 6-Zeilen-Parametertabelle; A1-Abschnitt unberührt.</done>
</task>

<task type="auto">
  <name>Task 2: IDLE/CANDIDATE/ACTIVE-Erstdefinition in fundamentals-2.typ sicherstellen</name>
  <files>T2000_Part1/chapters/fundamentals-2.typ</files>

  <read_first>
    T2000_Part1/chapters/fundamentals-2.typ — Zeile 38 (existierende Textstelle mit _IDLE_, _CANDIDATE_, _ACTIVE_ und Kurzerklärungen in Klammern) — vollständig lesen, um zu prüfen ob die Kurzerklärungen bereits ausreichend sind
    .planning/phases/40-dokumentation/40-AUDIT.md — Abschnitt "IDLE/CANDIDATE/ACTIVE — Erstauftreten und Definitionsstatus" — enthält Befund ob fundamentals-2.typ Zeile 38 DOKU-02 bereits erfüllt
  </read_first>

  <action>
  Lies Zeile 38 in fundamentals-2.typ. Die aktuelle Formulierung lautet:
  "_IDLE_ (Person nicht aktiv erkannt), _CANDIDATE_ (Person erkannt, Interaktionsabsicht wird durch den Gaze-Check geprüft) und _ACTIVE_ (Gaze-Check positiv, Sitzung läuft)"

  Prüfe: Sind alle drei Begriffe mit Kurzerklärung in Klammern eingeführt, BEVOR sie in Kap. 4 (methodology.typ) ohne Erklärung verwendet werden?

  Falls 40-AUDIT.md sagt "ERFÜLLT": Keine Änderung an fundamentals-2.typ notwendig. Dokumentiere in der SUMMARY, dass DOKU-02 durch Zeile 38 bereits abgedeckt ist.

  Falls 40-AUDIT.md sagt "FEHLT": Ergänze am angegebenen Einfügeort einen Satz, der die drei Begriffe definiert. Formulierungsvorlage (falls nötig):
  "Die Zustandsverwaltung folgt drei Zuständen: _IDLE_ (Person nicht aktiv erkannt), _CANDIDATE_ (Person erkannt, Interaktionsabsicht wird geprüft) und _ACTIVE_ (Interaktionsabsicht bestätigt, Sitzung aktiv)."

  Diese Formulierung steht im Gesamtarchitektur-Abschnitt (== Gesamtarchitektur und Systemüberblick) in fundamentals-2.typ, bevor die Begriffe in methodology.typ (Kap. 4) ohne Definition verwendet werden.

  Wenn die bestehende Zeile 38 ausreichend ist (was aufgrund der Analyse wahrscheinlich ist), schreibe die Datei nicht neu und vermerke den Status in der SUMMARY.
  </action>

  <verify>
    <automated>grep -n "IDLE.*Person nicht\|CANDIDATE.*Interaktion\|ACTIVE.*Sitzung" "T2000_Part1/chapters/fundamentals-2.typ"</automated>
  </verify>

  <acceptance_criteria>
  - In fundamentals-2.typ existiert eine Zeile, in der alle drei Begriffe IDLE, CANDIDATE und ACTIVE jeweils mit einer Kurzerklärung in Klammern eingeführt sind
  - Diese Zeile steht in Kap. 3 (fundamentals-2.typ), also vor Kap. 4 (methodology.typ) — die Reihenfolge der #include-Anweisungen in chapters/main.typ bestätigt: fundamentals-2.typ wird vor methodology.typ eingebunden
  - `grep -n "IDLE.*Person nicht" "T2000_Part1/chapters/fundamentals-2.typ"` liefert mindestens einen Treffer mit Zeilennummer
  </acceptance_criteria>

  <done>fundamentals-2.typ enthält die Erstdefinition aller drei Begriffe mit Kurzerklärung vor ihrem unkommentierten Gebrauch in Kap. 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit → Quelldateien | Schreibzugriff auf appendix.typ und fundamentals-2.typ; keine anderen Dateien |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02 | Tampering | appendix.typ | mitigate | A1-Tabelle explizit nicht anfassen; nur am Ende der Funktion ergänzen |
| T-40-03 | Tampering | fundamentals-2.typ | mitigate | Nur wenn 40-AUDIT.md Status = FEHLT; andernfalls keine Änderung |
| T-40-SC | Tampering | npm/pip/cargo | accept | Keine Package-Installs |
</threat_model>

<verification>
- Anhang A2 vorhanden: `grep "Systemparameter" "T2000_Part1/pages/appendix.typ" && echo PASS`
- Alle 6 Parameter in appendix.typ: `grep -c "CANDIDATE_SECS\|LEAVE_SECS\|SIMILARITY_THRESHOLD\|FRAME_INTERVAL\|GROUP_ARRIVAL_WINDOW_SECS\|EMA" "T2000_Part1/pages/appendix.typ"` → ≥ 6
- IDLE-Definition vorhanden: `grep -c "IDLE.*Person nicht" "T2000_Part1/chapters/fundamentals-2.typ"` → ≥ 1
- A1-Tabelle unberührt: `grep "appendix-ai-heading" "T2000_Part1/pages/appendix.typ" && echo PASS`
</verification>

<success_criteria>
1. pages/appendix.typ enthält Abschnitt A2 mit Tabelle aller 6 Systemparameter inkl. Wert, Einheit und Kap.-Verweis.
2. fundamentals-2.typ definiert IDLE, CANDIDATE und ACTIVE mit Kurzerklärung beim ersten Auftreten in Kap. 3, vor jedem unbegründeten Gebrauch in Kap. 4+.
</success_criteria>

<output>
Create `.planning/phases/40-dokumentation/40-02-SUMMARY.md` when done
</output>
