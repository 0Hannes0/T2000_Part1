---
phase: 40-dokumentation
plan: 03
type: execute
wave: 3
depends_on:
  - 40-02
files_modified:
  - .planning/phases/40-dokumentation/40-FINALCHECK.md
autonomous: true
requirements:
  - DOKU-01
  - DOKU-02

must_haves:
  truths:
    - "DOKU-01: appendix.typ enthält genau 6 Parameterzeilen — nicht weniger"
    - "DOKU-01: Jede Zeile hat Wert, Einheit und Kap.-Verweis — keine leeren Zellen"
    - "DOKU-02: IDLE, CANDIDATE, ACTIVE sind in fundamentals-2.typ vor methodology.typ definiert"
    - "DOKU-02: Kein IDLE/CANDIDATE/ACTIVE-Vorkommen in methodology.typ ohne vorangegangene Definition"
  artifacts:
    - path: ".planning/phases/40-dokumentation/40-FINALCHECK.md"
      provides: "PASS/FAIL für DOKU-01 und DOKU-02 mit Belegen"
  key_links:
    - from: "T2000_Part1/pages/appendix.typ"
      to: ".planning/phases/40-dokumentation/40-FINALCHECK.md"
      via: "grep-Ausgabe belegt 6 Parameter"
    - from: "T2000_Part1/chapters/fundamentals-2.typ"
      to: ".planning/phases/40-dokumentation/40-FINALCHECK.md"
      via: "grep-Ausgabe belegt Erstdefinition vor Kap. 4"
---

<objective>
Finalcheck: beide Success Criteria (DOKU-01 und DOKU-02) maschinell und textuell verifizieren; Ergebnis in 40-FINALCHECK.md dokumentieren.

Purpose: Phase 40 abschließen — sicherstellen, dass Wave 2 alle Anforderungen korrekt umgesetzt hat.
Output: 40-FINALCHECK.md mit PASS/FAIL je Kriterium und Beleg-Ausgaben der Verifikations-Kommandos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-dokumentation/40-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOKU-01 verifizieren — 6-Parameter-Tabelle vollständig</name>
  <files>.planning/phases/40-dokumentation/40-FINALCHECK.md</files>

  <read_first>
    T2000_Part1/pages/appendix.typ — vollständigen Inhalt lesen um Tabelle zu überprüfen
    .planning/phases/40-dokumentation/40-02-SUMMARY.md — was Wave 2 umgesetzt hat
  </read_first>

  <action>
  Führe diese Verifikationsschritte aus und dokumentiere die Ergebnisse in 40-FINALCHECK.md:

  1. Prüfe ob "A2: Systemparameter" in appendix.typ vorhanden ist.
     Kommando: `grep -n "A2.*Systemparameter\|Systemparameter" T2000_Part1/pages/appendix.typ`
     Erwartung: mindestens 1 Treffer.

  2. Zähle Parameterzeilen in der Tabelle: alle 6 Pflichtparameter müssen enthalten sein.
     Kommando: `grep -c "CANDIDATE_SECS\|LEAVE_SECS\|SIMILARITY_THRESHOLD\|FRAME_INTERVAL\|GROUP_ARRIVAL_WINDOW_SECS\|EMA" T2000_Part1/pages/appendix.typ`
     Erwartung: Ausgabe ≥ 6.

  3. Prüfe dass jeder Parameter einen Wert hat — keine leeren Tabellenfelder.
     Suche nach den konkreten Werten in appendix.typ: 4,0 / 10,0 / 0,52 / 1,0 / 2,0 / 0,2.
     Kommando: `grep -c "4,0\|10,0\|0,52\|1,0\|2,0\|0,2" T2000_Part1/pages/appendix.typ`
     Erwartung: ≥ 5 (0,2 und 2,0 könnten als Substring überlappen, daher ≥ 5).

  4. Prüfe dass Kap.-Verweise vorhanden sind.
     Kommando: `grep -c "Kap\." T2000_Part1/pages/appendix.typ`
     Erwartung: ≥ 6.

  5. Prüfe A1-Tabelle unberührt.
     Kommando: `grep -n "appendix-ai-heading\|A1.*KI" T2000_Part1/pages/appendix.typ`
     Erwartung: mindestens 1 Treffer.

  Schreibe für DOKU-01 entweder: "DOKU-01: PASS — [Beleg]" oder "DOKU-01: FAIL — [was fehlt]".
  </action>

  <verify>
    <automated>grep -c "DOKU-01.*PASS\|DOKU-01.*FAIL" ".planning/phases/40-dokumentation/40-FINALCHECK.md"</automated>
  </verify>

  <acceptance_criteria>
  - 40-FINALCHECK.md enthält "DOKU-01: PASS" oder "DOKU-01: FAIL" mit konkretem Beleg
  - Bei PASS: grep-Ausgaben der 5 Verifikationskommandos sind in der Datei dokumentiert
  - Bei FAIL: die fehlenden Elemente sind exakt benannt und Wave 2 muss nachgebessert werden (Executor-Instruktion: FAIL bedeutet sofortige Nachbesserung in appendix.typ vor SUMMARY-Erstellung)
  </acceptance_criteria>

  <done>DOKU-01-Verifikation in 40-FINALCHECK.md mit PASS oder dokumentiertem FAIL und Nachbesserung.</done>
</task>

<task type="auto">
  <name>Task 2: DOKU-02 verifizieren — Terminologie-Einführung vor erstem Gebrauch</name>
  <files>.planning/phases/40-dokumentation/40-FINALCHECK.md</files>

  <read_first>
    T2000_Part1/chapters/fundamentals-2.typ — Zeile 38 und Umgebung: enthält "_IDLE_ (Person nicht aktiv erkannt), _CANDIDATE_ (Person erkannt, Interaktionsabsicht wird durch den Gaze-Check geprüft) und _ACTIVE_ (Gaze-Check positiv, Sitzung läuft)"
    T2000_Part1/chapters/methodology.typ — Zeile 60 ("== State Machine: IDLE → CANDIDATE → ACTIVE") und Zeilen 87–89 (Zustandsknoten IDLE, CANDIDATE, ACTIVE) — zeigt erste Verwendung in Kap. 4 ohne Definition
    T2000_Part1/chapters/main.typ — Include-Reihenfolge: fundamentals-2.typ → methodology.typ (bestätigt dass Kap. 3 vor Kap. 4 kommt)
  </read_first>

  <action>
  Führe diese Verifikationsschritte aus:

  1. Prüfe Erstdefinition in fundamentals-2.typ.
     Kommando: `grep -n "IDLE.*Person nicht aktiv\|IDLE.*nicht aktiv" T2000_Part1/chapters/fundamentals-2.typ`
     Erwartung: mindestens 1 Treffer.

  2. Prüfe dass alle drei Begriffe mit Klammererklärung eingeführt sind.
     Kommando: `grep -n "_IDLE_\|_CANDIDATE_\|_ACTIVE_" T2000_Part1/chapters/fundamentals-2.typ | head -5`
     Erwartung: Alle drei Begriffe erscheinen in Zeile ≤ 45 von fundamentals-2.typ, jeweils mit Klammerinhalt dahinter.

  3. Prüfe Include-Reihenfolge: fundamentals-2.typ muss vor methodology.typ stehen.
     Kommando: `grep -n "fundamentals-2\|methodology" T2000_Part1/chapters/main.typ`
     Erwartung: fundamentals-2.typ hat kleinere Zeilennummer als methodology.typ.

  4. Prüfe erstes IDLE-Vorkommen in methodology.typ (Kap. 4).
     Kommando: `grep -n "IDLE\|CANDIDATE\|ACTIVE" T2000_Part1/chapters/methodology.typ | head -5`
     Erwartung: In methodology.typ werden die Begriffe ohne Klammerdefinition verwendet — das ist korrekt, weil die Definition bereits in fundamentals-2.typ erfolgt ist.

  Schreibe für DOKU-02 entweder: "DOKU-02: PASS — [Beleg]" oder "DOKU-02: FAIL — [was fehlt]".

  Abschlusssatz in 40-FINALCHECK.md:
  "Phase 40 Gesamtstatus: [PASS/FAIL] — DOKU-01 [PASS/FAIL], DOKU-02 [PASS/FAIL]."
  </action>

  <verify>
    <automated>grep -c "DOKU-02.*PASS\|DOKU-02.*FAIL" ".planning/phases/40-dokumentation/40-FINALCHECK.md"</automated>
  </verify>

  <acceptance_criteria>
  - 40-FINALCHECK.md enthält "DOKU-02: PASS" oder "DOKU-02: FAIL" mit Beleg
  - Bei PASS: grep-Ausgaben belegen (a) Definition in fundamentals-2.typ und (b) Include-Reihenfolge fundamentals-2.typ vor methodology.typ
  - "Phase 40 Gesamtstatus:" Zeile ist vorhanden
  - Bei FAIL in einem der Kriterien: Executor korrigiert die betroffene Stelle sofort (appendix.typ oder fundamentals-2.typ) vor der SUMMARY-Erstellung
  </acceptance_criteria>

  <done>40-FINALCHECK.md vollständig mit DOKU-01/DOKU-02 PASS/FAIL und Phase-40-Gesamtstatus-Zeile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quelldateien → Verifikation | Nur Lesen; Schreiben nur in 40-FINALCHECK.md und ggf. Nachbesserung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-04 | Information Disclosure | 40-FINALCHECK.md | accept | Nur lokale Planungsdatei, kein Netz |
| T-40-SC | Tampering | npm/pip/cargo | accept | Keine Package-Installs |
</threat_model>

<verification>
- Finalcheck-Datei vorhanden: `test -f ".planning/phases/40-dokumentation/40-FINALCHECK.md" && echo PASS`
- DOKU-01 PASS: `grep "DOKU-01.*PASS" ".planning/phases/40-dokumentation/40-FINALCHECK.md" && echo DOKU-01-OK`
- DOKU-02 PASS: `grep "DOKU-02.*PASS" ".planning/phases/40-dokumentation/40-FINALCHECK.md" && echo DOKU-02-OK`
- Gesamtstatus: `grep "Phase 40 Gesamtstatus" ".planning/phases/40-dokumentation/40-FINALCHECK.md" && echo STATUS-OK`
</verification>

<success_criteria>
40-FINALCHECK.md enthält "Phase 40 Gesamtstatus: PASS" mit grep-Belegen für beide Kriterien. Falls ein Kriterium FAIL ist, wurde die betreffende Datei nachgebessert und das FINALCHECK.md auf PASS aktualisiert.
</success_criteria>

<output>
Create `.planning/phases/40-dokumentation/40-03-SUMMARY.md` when done
</output>
