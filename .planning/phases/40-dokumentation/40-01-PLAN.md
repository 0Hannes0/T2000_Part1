---
phase: 40-dokumentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/40-dokumentation/40-AUDIT.md
autonomous: true
requirements:
  - DOKU-01
  - DOKU-02

must_haves:
  truths:
    - "Es ist bekannt, ob eine Anhang-A-Parametertabelle bereits existiert"
    - "Alle 6 Zielparameter sind mit Wert, Einheit und erstem Textauftreten kartiert"
    - "Die Zeilennummer des ersten IDLE/CANDIDATE/ACTIVE-Auftretens in jedem .typ-File ist dokumentiert"
    - "Es ist entschieden, ob fundamentals-2.typ Zeile 38 bereits als Erstdefinition ausreicht oder ob eine explizitere Einführung fehlt"
  artifacts:
    - path: ".planning/phases/40-dokumentation/40-AUDIT.md"
      provides: "Audit-Befund: Anhang-Status + 6-Parameter-Tabelle + IDLE/CANDIDATE/ACTIVE-Erstdefinitions-Befund"
  key_links:
    - from: "40-AUDIT.md"
      to: "40-02-PLAN.md"
      via: "liefert konkreten Einfügeort und Tabelleninhalt für Wave 2"
---

<objective>
Audit: feststellen ob Anhang A existiert; alle 6 Systemparameter mit Wert, Einheit und Kapitelreferenz kartieren; die früheste Textstelle für IDLE, CANDIDATE und ACTIVE in den .typ-Dateien lokalisieren und prüfen, ob dort bereits eine Definition steht.

Purpose: Wave 2 braucht exakte Insertionstellen und Parameterwerte — dieser Plan liefert sie.
Output: 40-AUDIT.md mit vollständiger Bestandsaufnahme.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anhang-Bestand und Parametersuche</name>
  <files>.planning/phases/40-dokumentation/40-AUDIT.md</files>

  <read_first>
    T2000_Part1/pages/appendix.typ — aktueller Anhang-Inhalt; prüfen ob ein Abschnitt "Systemparameter" bereits vorhanden ist
    T2000_Part1/template.typ — Zeile 175: #appendix(labels: labels) — zeigt wie Anhang eingebunden ist
    T2000_Part1/i18n/DE.typ — appendix-ai-heading Zeile 69 — zeigt Struktur des bestehenden Anhangs (A1: KI-Werkzeuge)
    T2000_Part1/chapters/methodology.typ — Zeilen 60–130 — Tab. 4.3 CANDIDATE_SECS/LEAVE_SECS/FRAME_INTERVAL + Tab. 4.4 GROUP_ARRIVAL_WINDOW_SECS
    T2000_Part1/chapters/practical-1.typ — Zeilen 88–140 — EMA-α=0,2 (Zeile 117–119), SIMILARITY_THRESHOLD (Zeile 28)
  </read_first>

  <action>
  Lies die oben genannten Dateien und erstelle 40-AUDIT.md mit folgenden Abschnitten:

  ## Anhang-Bestand
  Dokumentiere, was pages/appendix.typ aktuell enthält. Notiere: existiert ein Abschnitt "Systemparameter" oder "Anhang A" mit Parametertabelle? (Antwort ist mit hoher Wahrscheinlichkeit: Nein — nur A1 KI-Werkzeuge vorhanden.)

  ## Parameterübersicht (alle 6 Pflichtparameter)
  Erstelle eine Tabelle mit exakt diesen Spalten: Parameter | Wert | Einheit | Quelldatei+Zeile | Kapitelreferenz im Text

  Die 6 zu kartierenden Parameter (Werte aus den Quelldateien ablesen, nicht erraten):
  - CANDIDATE_SECS — in methodology.typ Tab. 4.3
  - LEAVE_SECS — in methodology.typ Tab. 4.3
  - SIMILARITY_THRESHOLD — in practical-1.typ Tab. 5.1 (Wert 0,52)
  - FRAME_INTERVAL — in methodology.typ Tab. 4.3
  - GROUP_ARRIVAL_WINDOW_SECS — in methodology.typ Tab. 4.4
  - EMA-α — in practical-1.typ Zeile ~117 (Wert 0,2; Einheit: dimensionslos)

  Kapitelreferenz = das Kapitel, in dem der Parameter primär eingeführt und erklärt wird (z.B. "Kap. 4.3", "Kap. 5.3").

  ## IDLE/CANDIDATE/ACTIVE — Erstauftreten und Definitionsstatus
  Notiere für jede der folgenden Dateien die erste Zeile, in der IDLE, CANDIDATE oder ACTIVE im Fließtext erscheint:
  - introduction.typ (Ergebnis: vermutlich kein Vorkommen)
  - fundamentals-1.typ (Ergebnis: vermutlich kein Vorkommen)
  - fundamentals-2.typ — Zeile 38 enthält die Definition "_IDLE_ (Person nicht aktiv erkannt), _CANDIDATE_ (Person erkannt, Interaktionsabsicht wird durch den Gaze-Check geprüft) und _ACTIVE_ (Gaze-Check positiv, Sitzung läuft)" — dokumentiere diese Zeile exakt

  Bewertung: Reicht fundamentals-2.typ Zeile 38 als Erstdefinition aus?
  Kriterium: Die drei Begriffe müssen mit Kurzerklärung in Klammern eingeführt sein, BEVOR sie in Kap. 4 (methodology.typ) oder später ohne Erklärung verwendet werden.
  Wenn Zeile 38 diese Bedingung erfüllt: Status = "ERFÜLLT". Wenn nicht (z.B. fehlt Erklärung oder Begriffe tauchen früher ohne Definition auf): Status = "FEHLT — Einfügeort: [Zeile X in Datei Y]".

  ## Befund und Wave-2-Handlungsanweisung
  Fasse zusammen:
  - Muss Anhang A neu angelegt werden? (Ja/Nein)
  - Ist die Parametertabelle vollständig erfasst? (Ja/Nein)
  - Ist DOKU-02 (Terminologie-Einführung) bereits erfüllt oder muss Wave 2 handeln?
  </action>

  <verify>
    <automated>grep -c "CANDIDATE_SECS\|LEAVE_SECS\|SIMILARITY_THRESHOLD\|FRAME_INTERVAL\|GROUP_ARRIVAL_WINDOW_SECS\|EMA" ".planning/phases/40-dokumentation/40-AUDIT.md"</automated>
  </verify>

  <acceptance_criteria>
  - 40-AUDIT.md existiert und enthält alle 6 Parameternamen
  - Jeder der 6 Parameter hat einen konkreten Wert (z.B. "4,0 s", "0,52", "0,2") aus dem Quelldokument, keine Platzhalter
  - Für IDLE/CANDIDATE/ACTIVE ist die erste Zeilennummer in fundamentals-2.typ notiert
  - Der Abschnitt "Befund und Wave-2-Handlungsanweisung" enthält eine klare Ja/Nein-Entscheidung für jeden der drei Punkte
  </acceptance_criteria>

  <done>40-AUDIT.md existiert mit Parameterübersicht (6 Einträge, alle mit Wert+Einheit+Quelle) und IDLE/CANDIDATE/ACTIVE-Befund inkl. Definitionsstatus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dokument → Audit | Nur Lesen bestehender .typ-Dateien; kein Schreiben in Quelldateien |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Information Disclosure | .planning/ Dateien | accept | Nur lokal, kein Netz, kein personenbezogenes Datum |
| T-40-SC | Tampering | keine npm/pip-Installs | accept | Keine Package-Manager-Aufrufe in diesem Plan |
</threat_model>

<verification>
- 40-AUDIT.md vorhanden: `test -f ".planning/phases/40-dokumentation/40-AUDIT.md" && echo PASS`
- Alle 6 Parameter enthalten: `grep -c "CANDIDATE_SECS\|LEAVE_SECS\|SIMILARITY_THRESHOLD\|FRAME_INTERVAL\|GROUP_ARRIVAL_WINDOW_SECS\|EMA" ".planning/phases/40-dokumentation/40-AUDIT.md"` → Ausgabe ≥ 6
</verification>

<success_criteria>
40-AUDIT.md liefert exakte Parameterwerte für alle 6 Pflichtparameter und einen eindeutigen Befund zum Definitionsstatus von IDLE/CANDIDATE/ACTIVE, sodass Wave 2 ohne weitere Recherche handeln kann.
</success_criteria>

<output>
Create `.planning/phases/40-dokumentation/40-01-SUMMARY.md` when done
</output>
